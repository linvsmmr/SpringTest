<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.eunbi.springtest.mybatis.test01.repository.RealEstateRepository">
    <select id="selectRealEstate" parameterType="int" resultType="com.eunbi.springtest.mybatis.test01.domain.RealEstate">
        SELECT
        `id`
        , `realtor_id`
        , `address`
        , `area`
        , `type`
        , `price`
        , `rent_price`
        , `created_at`
        , `updated_at`
        FROM
            `real_estate`
        WHERE
            `id` = #{id}
    </select>

    <select id="selectRealEstateRentPrice" parameterType="int" resultType="com.eunbi.springtest.mybatis.test01.domain.RealEstate">
        SELECT
            `id`
             , `realtor_id`
             , `address`
             , `area`
             , `type`
             , `price`
             , `rent_price`
             , `created_at`
             , `updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[`rent_price` < #{rentPrice}]]>
    </select>

    <select id="selectRealEstateAreaAndPrice" parameterType="map" resultType="com.eunbi.springtest.mybatis.test01.domain.RealEstate">
        SELECT
            `id`
             , `realtor_id`
             , `address`
             , `area`
             , `type`
             , `price`
             , `rent_price`
             , `created_at`
             , `updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[`area` >= #{area}]]>
        AND
            <![CDATA[`price` <= #{price}]]>
    </select>

    <insert id="insertRealEstate" parameterType="com.eunbi.springtest.mybatis.test01.domain.RealEstate">
        INSERT INTO
        `real_estate`
        (`realtor_id`
        , `address`
        , `area`
        , `type`
        , `price`)
        VALUES
            (
              #{realtorId}
            , #{address}
            , #{area}
            , #{type}
            , #{price}
            )
    </insert>

    <insert id="insertRealEstate2" parameterType="com.eunbi.springtest.mybatis.test01.domain.RealEstate">
        INSERT INTO
            `real_estate`
        (`realtor_id`
        , `address`
        , `area`
        , `type`
        , `price`
        , `rent_price`)
        VALUES
            (
              #{realtorId}
            , #{address}
            , #{area}
            , #{type}
            , #{price}
            , #{rentPrice}
            )
    </insert>

    <update id="updateRealEstate" parameterType="map">
        UPDATE
        `id`
        ,`type`
        ,`price`
        FROM
            `real_estate`
    </update>


</mapper>