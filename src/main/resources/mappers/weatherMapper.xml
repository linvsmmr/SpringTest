<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eunbi.springtest.thymeleaf.comprehension.repository.WeatherRepository">
    <select id="selectWeatherInfo" resultType="com.eunbi.springtest.thymeleaf.comprehension.domain.Weather">
        SELECT
            `id`
        ,`date`
        ,`weather`
        ,`temperatures`
        ,`precipitation`
        ,`micro_dust`
        ,`wind_speed`
        ,`created_at`
        ,`updated_at`
        FROM
            `weatherhistory`
    </select>


    <insert id="insertWeather" parameterType="com.eunbi.springtest.thymeleaf.comprehension.domain.Weather">
        INSERT INTO
        `weatherhistory`
        (`date`
        ,`weather`
        ,`temperatures`
        ,`precipitation`
        ,`micro_dust`
        ,`wind_speed`
        ,`updated_at`)
        VALUES
            (#{date}
            ,#{weather}
            ,#{temperatures}
            ,#{precipitation}
            ,#{microDust}
            ,#{windSpeed}
            ,#{updatedAt})
    </insert>


</mapper>